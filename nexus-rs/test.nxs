// Comment.
fn free(a, b, c) -> Int {
  return 42 + a + b + c; // Result comment.
}

/* Multi-line
   comment

   fn should_be_ignored() { return 10; }
   */
fn main() {
  fn local1() {
    return "Hello1".length();
  }

  let local2 = ||{ return "Hello2".length(); };

  let x = free(1, 2.889, local1() + local2());

  let a = node("Input");
  let b = node("Output");
  a.Out -> b.In;

  let mut c = module("Subsystem");
  for i in 0..=3 {
    let p = node("Processor");
    p.Input -> b.Output;
    c.push(p);
  }

  print "\\\\\" End:\t\"" + |x|{ return 17 + x; /* inline comment */ }() + "\"";

  || /* sdfsdf
  sdf
  sdf
  sdf
  */ {}
}
